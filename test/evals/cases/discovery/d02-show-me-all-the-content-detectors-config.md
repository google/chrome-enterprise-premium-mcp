---
id: d02
category: discovery
tags:
  - discovery
expected_tools:
  - list_detectors
required_patterns:
  - Fake URL Detector
---

## Prompt

Show me all the content detectors configured in my environment.

## Golden Response

3 detectors found: "Fake URL Detector" (URL list targeting malware.com), "End-to-End Temp Detector" (URL list targeting temp.com), and "Fake Regex Detector" (regex pattern).
